# Purpose: Acts as the entry point for your application. It will set up a basic web server using Flask (a lightweight Python web framework). This server will handle HTTP requests for data collection and serve the frontend web app.
# Interacts with: data_processing.py, google_maps.py, database.py, /templates/index.html.
# Programming Language: Python (Flask).